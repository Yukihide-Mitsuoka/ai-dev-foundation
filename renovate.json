{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Primary dependency automation (SEC-030/031). Dependabot (.github/dependabot.yml) is kept for GitHub-native security alerts only.",
  "extends": [
    "config:best-practices",
    ":semanticCommits",
    ":dependencyDashboard",
    "helpers:pinGitHubActionDigests",
    "schedule:weekly"
  ],
  "labels": ["type:dependencies"],
  "packageRules": [
    {
      "description": "Group non-major updates into one weekly PR to respect GR-020 review capacity",
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "non-major dependencies"
    },
    {
      "description": "Major updates land one-per-PR with the COD-040 justification filled by the reviewer",
      "matchUpdateTypes": ["major"],
      "dependencyDashboardApproval": true
    }
  ],
  "vulnerabilityAlerts": {
    "labels": ["type:security"],
    "prPriority": 10
  },
  "osvVulnerabilityAlerts": true
}
